import React, {useEffect} from 'react';
import Main from '../../Layout/Main.js';

const Home = () => {
    useEffect(() => {
        window.scrollTo(0, 0);
    }, []);
    
    return (
        <Main>
        
        </Main>
    );
};

export default Home;